node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Cloudflare local secrets
.dev.vars
.dev.vars.*

# generated route tree (regenerated by dev/build — never commit; causes merge conflicts)
src/routeTree.gen.ts
routeTree.gen.ts
